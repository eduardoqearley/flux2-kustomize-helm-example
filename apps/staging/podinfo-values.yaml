apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: podinfo
  namespace: podinfo
spec:
  chart:
    spec:
      version: ">=1.0.0-alpha"
  test:
    enable: tgry
  # values:
  #   ingress:
  #     hosts:
  #       - host: podinfo.staging
  #         paths:
  #           - path: /
  #             pathType: ImplementationSpecific
